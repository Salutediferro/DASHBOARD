import { SetPasswordForm } from "./set-password-form";

export const metadata = { title: "Imposta password — Salute di Ferro" };

export default function SetPasswordPage() {
  return <SetPasswordForm />;
}
