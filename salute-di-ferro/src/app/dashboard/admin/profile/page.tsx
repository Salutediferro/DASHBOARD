import { ProfileForm } from "@/components/profile/profile-form";

export const metadata = { title: "Profilo admin — Salute di Ferro" };

export default function AdminProfilePage() {
  return <ProfileForm />;
}
