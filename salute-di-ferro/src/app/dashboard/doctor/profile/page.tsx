import { ProfileForm } from "@/components/profile/profile-form";

export const metadata = { title: "Profilo medico — Salute di Ferro" };

export default function DoctorProfilePage() {
  return <ProfileForm />;
}
